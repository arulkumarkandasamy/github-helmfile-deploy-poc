# image:
#   repository: ghcr.io/arulkumarkandasamy/python
#   pullPolicy: Always
#   # Overrides the image tag whose default is the chart appVersion.
#   tag: "latest"

# imagePullSecrets: [name: regcred]

# container:
#   port: 5000

image:
  repository: ghcr.io/arulkumarkandasamy/python
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

imagePullSecrets: [name: regcred]

container:
  port: 5000

# image:
#   repository: ghcr.io/arulkumarkandasamy/python
#   tag: sha-7bdbdf1
#   pullPolicy: Always
#   pullSecrets:
#     - "regcred"

# replicaCount: 1
 
# serviceAccountName: my-service-account

# dockercfg:
#   enabled: false
#   image:
#     pullSecret:
#       registry: https://ghcr.io/
#       username: arulkumarkandasamy
#       password: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
